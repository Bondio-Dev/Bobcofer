services:
  bobcofer-bot:
    build: .
    container_name: bobcofer-bot
    network_mode: "host"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DISPLAY=:0
      - BROWSER=google-chrome
      # Для myuser файл .Xauthority будет работать автоматически
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /home/myuser/.Xauthority:/root/.Xauthority:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - ./scheduled.json:/app/scheduled.json
      - ./contacts.json:/app/contacts.json
      - ./conf.json:/app/conf.json
      - ./token.json:/app/token.json
